spring:
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false

    deployer:
      openshift:
        namespace: scdf

logging:
  level:
    org:
      springframework:
        cloud:
          deployer: trace
    io:
      fabric8: trace
